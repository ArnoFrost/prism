# Prism Skills Catalog — governance metadata SSOT
# Owns Skill identity, visibility, stability, source, review and lifecycle
# metadata. It does not define the current distribution profile; that authority
# belongs only to dist-whitelist.yaml.
# 不定义当前 distribution profile；分发权威仅为 dist-whitelist.yaml。

version: "1.0.0"

policy:
  default_visibility: internal
  default_stability: experimental
  public_requires_audit: true
  note: "Private skills are unrestricted. Public skills require universality and normative necessity. Catalog governance does not imply distribution."

# 版本策略（SSOT）
# ─────────────────────────────────────────────
# public  → prism-* 前缀，对外暴露，version = 仓库 tag（如 0.5.0），repo bump 时统一 bump
# dev     → prism-* 前缀，不暴露，维护者内部工具，version = dev-NN，内部耦合演进
# common  → 无 prism-* 前缀，泛化技能，用户可自选装入，version = 独立 semver
# personal variant → dev-NN-{user}，基于 dev 方案加用户后缀
# 开源过渡 → v1.0.0 时 dev/common skill 如需 public 化，进入 catalog + 重置为 semver
versioning:
  public: "{repo_tag}"
  dev: "dev-{NN}"
  common: "independent semver"
  personal: "dev-{NN}-{user}"
  note: "NN starts at 01 per skill, increments on substantive change. Public versions bump atomically with repo tag."

# ─────────────────────────────────────────────
# Catalog entries（3.x legacy 条目已随 prism-4 分支剔除；终态见 git tag legacy-3x-final）
# ─────────────────────────────────────────────
entries:
# ─────────────────────────────────────────────
# Prism 4.0 SDK skill inventory — distribution is selected by dist-whitelist
# ─────────────────────────────────────────────
  - id: prism
    visibility: dev
    stability: experimental
    source_repo: prism
    reviewed_by: ArnoFrost
    reviewed_at: "2026-08-29"
    rationale: "Experimental effect-first state facade used by the natural dogfood profile; distribution remains solely owned by dist-whitelist.yaml and does not imply stable cutover."
  - id: prism-review
    visibility: dev
    stability: experimental
    source_repo: prism
    reviewed_by: ArnoFrost
    reviewed_at: "2026-08-14"
    rationale: "Prism 4.0 Review capability; emits Findings without authorization side effects."
  - id: prism-plan
    visibility: dev
    stability: experimental
    source_repo: prism
    reviewed_by: ArnoFrost
    reviewed_at: "2026-08-19"
    rationale: "Prism 4.0 Plan capability; actively designs advisory action structure without redefining Intent, producing Findings, committing Decisions, executing work, or implying fixed workflow order."

# ─────────────────────────────────────────────
# External prism-skills entries（独立仓库，随其自身发布）
# ─────────────────────────────────────────────
  - id: prism-maintain
    visibility: dev
    stability: experimental
    source_repo: prism-skills
    reviewed_by: ArnoFrost
    reviewed_at: "2026-08-20"
    rationale: "Personal multi-device maintenance router across configured SDK, Skills, Env and Workspace Git; clean code-only pulls may use bin/update directly."
  - id: update-marketplaces
    visibility: dev
    stability: experimental
    source_repo: prism-skills
    reviewed_by: ArnoFrost
    reviewed_at: "2026-04-23"
    rationale: "Unified plugin marketplace updater — Claude Code git repos + CodeBuddy zip markets. Replaces ArnoDotFiles legacy skill."
