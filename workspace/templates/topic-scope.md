---
date: YYYY-MM-DD
status: active
type: scope
kind: governance
tags:
  - {topic-tag}
related:
  - "./references/intake.md"
  - "./focus.md"
---

# Scope — {topic-name}

> 合同面 SSOT（retention=persistent）。review 不直接改，通过 dXX 间接驱动。
> focus（短期切片）与 structures/task.index（长期分解）的唯一上游。

> [!important] canonical form（scope 形态硬约束，详规 scope-templates.md §可读性度量）
> - **段落白名单**：恰含 6 段「目标 / 非目标 / 验收口径 / 关键约束 / 未决问题 / 变更记录」，禁新增未知二级标题。
> - **只 what 不 how**：禁类名/方法签名/文件路径等代码级细节（→ focus / structures/task-N / dXX）。
> - **可溯源**：每条 `V` 带 `Vn` 编号、每条 `OQ` 带 `OQ-n` 编号；勾选项注明触发 `dXX`/`rXX`。
> - **复杂度建议上限**（advisory，超限提示拆分）：总行数 ≤60 / V ≤8 / 约束 ≤5 / OQ ≤5。
> - **递归入口**：某 `V` 深化到自带 scope+wave（S3）时，膨胀 `structures/task-N`（task-scope 1:1 投影该 V），scope 本身不展开 how。

## 目标（G）

- G1:

## 非目标

-

## 验收口径（V）

- [ ] V1:

## 关键约束

-

## 未决问题（OQ）

- [ ] OQ-1:

## 变更记录

| 日期 | 触发 | 摘要 |
|------|------|------|
| YYYY-MM-DD | intake | 初始合同 |
